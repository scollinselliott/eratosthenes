% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eratosthenes.R
\name{absolute}
\alias{absolute}
\alias{absolute.character}
\title{Create an Absolute Constraint Object}
\usage{
absolute(id, assoc, type = NULL, samples)

\method{absolute}{character}(id, assoc, type = NULL, samples)
}
\arguments{
\item{id}{a \code{character} object, giving a unique ID of the constraint}

\item{assoc}{the element within an \code{events} object to which the constraint is associated}

\item{type}{(optional) a \code{character} object, giving the type of constraint (e.g., a ceramic type, coin, radiocarbon dat). Mutiple types/subtypes/classes can be given as a vector. Default is \code{NULL}.}

\item{samples}{a vector of samples drawn from the appertaining probability density function of that constraint}
}
\value{
An \code{absolute} object.
}
\description{
Analogous to the \code{\link[base]{list}} function, to create absolute constraint (\emph{terminus post quem} or \emph{ante quem}). The constraint must contain named elements of \code{"id"}, \code{"assoc"}, and \code{"samples"}, with an option to indicate \code{"type"}.
}
\examples{
# external constraints
coin1 <- absolute(id = "coin1", assoc = "B", samples = runif(100,-320,-300))
coin2 <- absolute(id = "coin2", assoc = "G", type = "RIC2 57", samples = seq(37, 41, length = 100))
  # seq(37, 41, length = 100) is equivalent in concept to runif(100, 37, 41))
destr <- absolute(id = "destr", assoc = "J", samples = 79)

coin1
coin2
destr

}
